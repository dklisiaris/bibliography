<%= cell(:navigation).call(:content_header, 
  title: t('import.import_data'),
  breadcrumbs: {
    fa('home') => root_url, 
    t('site_settings') => "#", 
    t('import.import_label') => nil
  }) %>

<%= render 'form' %>
