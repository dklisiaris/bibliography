<% pages = {fa('home') => root_url, t('site_settings') => "#", t('tasks.label') => nil} %>

<%= cell(:navigation).call(:content_header, title: t('tasks.label'), breadcrumbs: pages) %>

<%= render partial: "content_update_task", locals: {is_limited: true} %>



