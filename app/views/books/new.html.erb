<% pages = {fa('home') => root_url, t('books.label') => books_url, t('books.new') => nil} %>

<%= cell(:navigation).call(:content_header, title: t('books.new'), breadcrumbs: pages) %>

<%= render 'form' %>

<%= link_to t('back'), books_path, class: "label label-default" %>
