<%# Rails flash messages for noty library %>
<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <script>notify(<%= "\'#{msg}\'".html_safe %>,<%= name.to_s == 'notice' ? "\'success\'".html_safe : "\'error\'".html_safe %>);</script>  
  <% end %>
<% end %>
