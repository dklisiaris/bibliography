<%= form_for(@publisher) do |f| %>
  <% if @publisher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@publisher.errors.count, "error") %> prohibited this publisher from being saved:</h2>

      <ul>
      <% @publisher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field col-xs-6 form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="field col-xs-6 form-group">
    <%= f.label :owner %><br>
    <%= f.text_field :owner, class: 'form-control' %>
  </div>
  <div class="actions col-xs-6 form-group">
    <%= f.submit 'Save', class: 'form-control btn-success' %>
  </div>
<% end %>
