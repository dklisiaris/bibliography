$('.pages.welcome_guide').ready(function() {
  FormsWizard.init();

  window.recommendationLabels = <%= {
    recommend: I18n.t("books.recommend"),
    notRecommend: I18n.t("books.not_recommend"),
    removeRecommendation: I18n.t("books.remove_recommendation")
  }.to_json.html_safe %>;

  window.favouriteCategoryLabels = <%= {
    addToFavourites: I18n.t("categories.add_to_favourites"),
    favoured: I18n.t("categories.favoured")
  }.to_json.html_safe %>;

  $('.featured-category').click(function(){
    $( this ).toggleClass( "selected" );
  })
});

var FormsWizard = function() {

  return {
    init: function() {
      var currentStep = 1;

      // Get the progress bar and change its width when a step is shown
      var progressBar = $('#progress-bar-wizard');
      progressBar
        .css('width', '33%')
        .attr('aria-valuenow', '33');

      $("#progress-second, #progress-third").hide();

      $("#next2").click(function(){
        if(currentStep < 3){
          currentStep++;
          handleStepChange();
        }
      });

      $("#back2").click(function(){
        if(currentStep > 1){
          currentStep--;
          handleStepChange();
        }
      });

      var handleStepChange = function(){
        if (currentStep === 1) {
          $("#progress-second, #progress-third").hide();
          $("#progress-first").show();
          progressBar
            .css('width', '33%')
            .attr('aria-valuenow', '33')
            .removeClass('bg-info bg-success')
            .addClass('bg-danger');
        } else if (currentStep === 2) {
          $("#progress-first, #progress-third").hide();
          $("#progress-second").show();
          progressBar
            .css('width', '66%')
            .attr('aria-valuenow', '66')
            .removeClass('bg-danger bg-success')
            .addClass('bg-info');

          $('#rateable-books-wrapper').empty();
          $('#rateable-books-wrapper').addClass('loading');

          var token = '';
          var email = '';

          var email = $("input[name='email']").val();
          var token = $("input[name='token']").val();

          $.ajax({
            url: '/api/v1/books/rated_ids/',
            beforeSend: function (request) {
              request.setRequestHeader("Authorization", 'Token token="'+token+'", email="'+email+'"');
            },
          }).done(function(response){
            var liked_book_ids = response.liked_book_ids;
            var disliked_book_ids = response.disliked_book_ids;

            $.ajax({
              url: '/api/v1/categories/liked_with_books/',
              beforeSend: function (request) {
                request.setRequestHeader("Authorization", 'Token token="'+token+'", email="'+email+'"');
              },
            }).done(function(response) {
              $.each(response.categories, function(index, category) {
                var books_array = [];
                var books_html;
                if(category.books.length > 0) {
                  $.each(category.books, function(index, book) {
                    var like_btn_klass = ($.inArray(book.id, liked_book_ids) > -1) ? 'btn-success' : 'btn-outline-secondary';
                    var dislike_btn_klass = ($.inArray(book.id, disliked_book_ids) > -1) ? 'btn-danger' : 'btn-outline-secondary';

                    var book_html = "\
                    <div class='rateable-book'>\
                    <div class='book-cover'>\
                    <img height='114' width='114' title='"+ book.title +"' src='"+ book.cover +"' alt='book_cover' class='img-thumbnail' data-bs-toggle='tooltip' data-bs-placement='top' />\
                    </div>\
                    <button name='button' type='button' onclick='javascript:like("+book.id+")' class='btn btn-sm btn-like btn-like-"+book.id+" "+like_btn_klass+"'>      \
                      <i class='fa fa-thumbs-up'></i>\
                    </button>\
                    <button name='button' type='button' onclick='javascript:dislike("+book.id+")' class='btn btn-sm btn-dislike btn-dislike-"+book.id+" "+dislike_btn_klass+"'>      \
                      <i class='fa fa-thumbs-down'></i>\
                    </button>\
                    </div>\
                    ";
                    books_array.push(book_html);
                  });
                  books_html = books_array.join("");
                }
                else {
                  books_html = "\
                  <div class='text-center'>\
                    <span class='text-muted'>\
                    <small>Δεν υπάρχουν βιβλία ακόμα.</small>\
                    </span>\
                  </div>\
                  ";
                }

                $('#rateable-books-wrapper').append("\
                  <section class='static-page__wizard-category'>\
                    <header class='static-page__wizard-category-header'>"+ category.name +"</header>\
                    <div class='static-page__wizard-category-body'>\
                      <div class='book-container'>" + books_html + "</div>\
                    </div>\
                  </section>\
                ");

              });
              $('#rateable-books-wrapper').removeClass('loading');
              $('.book-container').masonry({
                itemSelector: '.rateable-book',
                columnWidth: 44,
                gutter: 12,
                fitWidth: true
              });
            });
          })
        } else if (currentStep === 3) {
          $("#progress-first, #progress-second").hide();
          $("#progress-third").show();
          progressBar
            .css('width', '100%')
            .attr('aria-valuenow', '100')
            .removeClass('bg-danger bg-info')
            .addClass('bg-success');
          window.location.replace(window.location.origin);
        }
      }
    }
  };
}();
